### Tailwind CSS (BootStrap)

- 유틸리티 퍼스트 방식으로 설계된 CSS 프레임워크
- BootStrap처럼 미리 만들어진 컴포넌트를 제공하는 대신, 테일윈드는 아주 작은 유틸리티 클래스를 제공
- 유틸리티 퍼스트: 작고 단일한 역할만 하는 CSS 클래스를 조합해서 UI를 만드는 방식

```
git clone https://github.com/SangYoonLee1231/react19-with-tailwind_startup-pack
```

- Tailwind의 유틸리티 클래스 네임은 꽤 직관적

- Tailwind CSS -> tailwind-merge

### CSS-in-JS (CSS를 자바스크립트가 생성)

- styled-components, emotion, vanilla extract

- vanilla extract -> zero-run-time

### 폰트